# Versions are those verified working in this environment (Python 3.12, CUDA 12.4).
# torch/torchvision carry the +cu124 local tag; install them from the PyTorch
# index if recreating the environment:
#   pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchvision==0.21.0
timm==1.0.16

# numpy pinned <2.4 because numba/umap-learn require it; 2.4.6 gets downgraded
# to 2.3.5 on install of umap-learn. The pipeline is verified on 2.3.5.
numpy==2.3.5
pandas==2.2.3
scikit-learn==1.7.0
pillow==12.2.0
matplotlib==3.10.3
seaborn==0.13.2
tqdm==4.66.4

# Dataset analysis (notebook 02)
umap-learn==0.5.12
imagehash==4.3.2
scikit-image==0.25.2

# Notebook
notebook==7.4.5
nbformat==5.10.4
ipywidgets
